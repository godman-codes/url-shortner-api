import styled from "styled-components";

export const Wrapper = styled.div`
   opacity: 1;
   text-align: center;
   min-width: 100px;
   position: absolute;
   color: var(--white);
   right: auto;
`;
